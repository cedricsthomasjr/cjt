import Hero from "@/components/Hero";

/**
 * A front door and nothing else. The record moved to the resume, the
 * affiliations and the current role moved to about, and the page index is what
 * the navigation was already for.
 */
export default function Home() {
  return (
    <main>
      <Hero />
    </main>
  );
}
